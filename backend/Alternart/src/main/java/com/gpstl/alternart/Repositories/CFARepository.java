package com.gpstl.alternart.Repositories;

import com.gpstl.alternart.Models.CFA;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CFARepository extends JpaRepository<CFA, Long> {
}